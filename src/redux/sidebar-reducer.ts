type InitialStateType = typeof initialState;

let initialState = {};
const sitebarReducer = (
  state = initialState,
  action: any
): InitialStateType => {
  return state;
};

export default sitebarReducer;
